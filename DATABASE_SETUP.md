# إعداد قاعدة البيانات والمصادقة

## ✅ ما تم إنجازه:

### 1. قاعدة البيانات (MongoDB)
- ✅ تم إنشاء `server/database.ts` مع:
  - User Schema
  - OTP Schema
  - Conversation Schema
  - Message Schema
  - Contact Schema

### 2. نظام المصادقة
- ✅ تم إنشاء `server/auth.ts` مع:
  - إرسال OTP عبر البريد الإلكتروني
  - التحقق من OTP
  - البحث عن المستخدمين المسجلين

### 3. API Routes
- ✅ `/api/auth/send-otp` - إرسال كود التحقق
- ✅ `/api/auth/verify-otp` - التحقق من كود التحقق
- ✅ `/api/users/find-by-phones` - البحث عن المستخدمين المسجلين

### 4. صفحة تسجيل الدخول
- ✅ تم إنشاء `app/auth/LoginPage.tsx`
- ✅ تم إنشاء `app/auth/page.tsx`

## ⚠️ ما يحتاج إعداد:

### 1. متغيرات البيئة (.env)
يجب إضافة هذه المتغيرات:
```
MONGODB_URI=mongodb://localhost:27017/royal-chat
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
```

### 2. قاعدة البيانات MongoDB
- يجب تثبيت MongoDB محلياً أو استخدام MongoDB Atlas
- أو إضافة MongoDB إلى Railway

### 3. إعداد البريد الإلكتروني
- يجب إعداد SMTP لإرسال OTP
- يمكن استخدام Gmail أو أي خدمة SMTP أخرى

## 📝 الخطوات التالية:

1. إضافة متغيرات البيئة
2. ربط قاعدة البيانات
3. اختبار نظام المصادقة
4. إزالة البيانات الوهمية
5. ربط مزامنة جهات الاتصال

