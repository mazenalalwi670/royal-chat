# 🔗 روابط الوصول السريع - Quick Access Links

## 👤 **لوحة تسجيل دخول المستخدمين العاديين**

### 🔗 **الرابط المباشر:**
```
http://localhost:4000
```

### 📝 **كيفية الوصول:**
1. تأكد من أن الخادم يعمل:
   ```bash
   npm run dev
   ```
   أو
   ```bash
   npm run dev:all
   ```

2. افتح المتصفح واذهب إلى:
   ```
   http://localhost:4000
   ```

3. ستظهر صفحة تسجيل دخول فاخرة مع:
   - خلفية بيضاء أنيقة
   - جسيمات ذهبية متحركة
   - تاج ذهبي
   - اسم التطبيق "ROYAL CHAT"
   - حقل إدخال رقم الهاتف

4. أدخل أي رقم هاتف (مثال: `966501234567`)
5. اضغط "دخول"
6. سيتم توجيهك إلى التطبيق

---

## 👑 **لوحة تسجيل دخول المالك**

### 🔗 **الرابط المباشر:**
```
http://localhost:4001/admin
```

### 📝 **كيفية الوصول:**
1. تأكد من أن الخادم يعمل:
   ```bash
   npm run dev:admin
   ```
   أو
   ```bash
   npm run dev:all
   ```

2. افتح المتصفح واذهب إلى:
   ```
   http://localhost:4001/admin
   ```

3. ستظهر صفحة تسجيل دخول فاخرة مع:
   - خلفية ذهبية متحركة
   - تاج متحرك كبير
   - عنوان "👑 المالك 👑"
   - حقول إدخال اسم المستخدم وكلمة المرور

4. أدخل:
   - **اسم المستخدم:** `admin`
   - **كلمة المرور:** `mazen@771885223`

5. اضغط "دخول المالك"
6. سيتم توجيهك إلى لوحة تحكم المالك

---

## 🚀 **تشغيل كل شيء معاً**

### لتشغيل جميع الخوادم:
```bash
npm run dev:all
```

هذا الأمر سيشغل:
- ✅ خادم المستخدمين العاديين على **المنفذ 4000**
- ✅ خادم المالك على **المنفذ 4001**
- ✅ خادم WebSocket

### بعد التشغيل:
- **المستخدمون:** http://localhost:4000
- **المالك:** http://localhost:4001/admin

---

## 📋 **ملخص الروابط**

| النوع | الرابط | المنفذ |
|------|--------|--------|
| 👤 المستخدمين العاديين | `http://localhost:4000` | 4000 |
| 👑 المالك | `http://localhost:4001/admin` | 4001 |

---

## ⚠️ **ملاحظات مهمة**

1. **يجب تشغيل الخوادم أولاً:**
   - استخدم `npm run dev:all` لتشغيل كل شيء
   - أو شغّل كل خادم منفصلاً

2. **لوحة التحكم محمية:**
   - فقط المالك يمكنه الوصول إلى `http://localhost:4001/admin`
   - المستخدمون العاديون لا يمكنهم الوصول إلى لوحة التحكم

3. **المالك مخفي:**
   - عند تسجيل دخول المالك، يكون مخفياً افتراضياً
   - يمكنه رؤية كل شيء بينما هو مخفي

---

**استمتع باستخدام Royal Chat! 👑**

