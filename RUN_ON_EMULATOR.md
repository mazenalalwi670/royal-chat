# 📱 تشغيل التطبيق على محاكي الهاتف

## 🎯 الخطوات الكاملة:

---

## 1️⃣ تشغيل التطبيق على الكمبيوتر

### أ) افتح Terminal في مجلد المشروع:

```bash
cd C:\Royal
```

### ب) ثبت التبعيات (إذا لم تكن مثبتة):

```bash
npm install
```

### ج) شغّل التطبيق:

```bash
npm run dev
```

**ستظهر رسالة مثل:**

```
> Ready on http://localhost:4000
```

---

## 2️⃣ احصل على IP المحلي للكمبيوتر

### على Windows:

#### الطريقة 1: من Command Prompt

```bash
ipconfig
```

**ابحث عن:**

```
IPv4 Address . . . . . . . . . . . : 192.168.1.XXX
```

#### الطريقة 2: من PowerShell

```powershell
Get-NetIPAddress -AddressFamily IPv4 | Where-Object {$_.IPAddress -like "192.168.*"}
```

**ستحصل على IP مثل:** `192.168.1.100`

---

## 3️⃣ شغّل التطبيق على IP المحلي

### أ) أوقف التطبيق (اضغط Ctrl+C)

### ب) شغّله مرة أخرى على IP المحلي:

```bash
npm run dev
```

**التطبيق سيعمل على:** `http://192.168.1.XXX:4000`

---

## 4️⃣ افتح التطبيق من المحاكي

### على محاكي Android (Android Studio Emulator):

#### أ) افتح Android Studio Emulator

#### ب) افتح متصفح في المحاكي (Chrome)

#### ج) اكتب الرابط:

```
http://192.168.1.XXX:4000
```

**مثال:**

```
http://192.168.1.100:4000
```

---

## 5️⃣ إعداد WebSocket للعمل على الشبكة المحلية

### المشكلة:

- WebSocket يحتاج أن يعمل على نفس IP المحلي
- التطبيق يستخدم `localhost` افتراضياً

### الحل:

#### أ) أنشئ ملف `.env.local` في مجلد المشروع:

```env
NEXT_PUBLIC_APP_URL=http://192.168.1.XXX:4000
NEXT_PUBLIC_WS_URL=ws://192.168.1.XXX:4000
ALLOWED_ORIGINS=http://192.168.1.XXX:4000
```

**استبدل `192.168.1.XXX` بـ IP المحلي الفعلي!**

#### ب) أعد تشغيل التطبيق:

```bash
npm run dev
```

---

## 6️⃣ اختبر التطبيق على المحاكي

### ✅ اختبارات:

- [ ] افتح الرابط في متصفح المحاكي
- [ ] سجّل الدخول برقم هاتف
- [ ] أرسل رسالة
- [ ] أرسل صورة
- [ ] سجّل صوت

---

## 🔧 حل المشاكل الشائعة:

### المشكلة 1: المحاكي لا يفتح الرابط

**الحل:**

- تأكد من أن الكمبيوتر والمحاكي على نفس الشبكة
- تأكد من أن Firewall لا يحجب المنفذ 4000
- جرب `http://10.0.2.2:4000` (للمحاكي Android)

### المشكلة 2: WebSocket لا يعمل

**الحل:**

- تأكد من ملف `.env.local` موجود
- تأكد من أن IP صحيح
- أعد تشغيل التطبيق بعد تعديل `.env.local`

### المشكلة 3: الصور لا تُرسل

**الحل:**

- هذا طبيعي - الصور تُحفظ كـ base64 محلياً
- يجب أن تعمل في الإنتاج على Railway

---

## 📱 استخدام محاكي Android Studio:

### 1. تثبيت Android Studio:

- [ ] حمّل من: https://developer.android.com/studio
- [ ] ثبت Android Studio
- [ ] افتح Android Studio

### 2. إنشاء محاكي:

- [ ] اضغط "Device Manager"
- [ ] اضغط "Create Device"
- [ ] اختر جهاز (مثل Pixel 5)
- [ ] اختر نظام (مثل Android 13)
- [ ] اضغط "Finish"

### 3. تشغيل المحاكي:

- [ ] اضغط زر Play بجوار المحاكي
- [ ] انتظر حتى يفتح المحاكي

### 4. فتح المتصفح في المحاكي:

- [ ] في المحاكي، افتح Chrome
- [ ] اكتب الرابط: `http://192.168.1.XXX:4000`

---

## 🚀 طريقة سريعة (Script):

### أنشئ ملف `start-emulator.bat`:

```batch
@echo off
echo Getting local IP address...
for /f "tokens=2 delims=:" %%a in ('ipconfig ^| findstr /c:"IPv4"') do (
    set IP=%%a
    goto :found
)
:found
set IP=%IP:~1%
echo Local IP: %IP%
echo.
echo Starting app on http://%IP%:4000
echo.
echo Open this URL in emulator: http://%IP%:4000
echo.
npm run dev
```

**شغّل الملف:** اضغط مرتين على `start-emulator.bat`

---

## ✅ الخلاصة:

1. **شغّل التطبيق:** `npm run dev`
2. **احصل على IP المحلي:** `ipconfig`
3. **أنشئ `.env.local`** مع IP المحلي
4. **افتح المحاكي** وافتح الرابط في المتصفح
5. **اختبر التطبيق!**

---

**جاهز للاختبار على المحاكي! 📱**
